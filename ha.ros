#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(print (defpackage :ros.script.ha.3827089312
  (:use :cl)))
(print (in-package :ros.script.ha.3827089312))

(load "x")

(defun main (&rest argv)
  (declare (ignorable argv))
  (format t "ha!~%")
  (haha))
;;; vim: set ft=lisp lisp:
